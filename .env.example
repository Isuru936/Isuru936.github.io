# Turso (libSQL). Values from:
#   turso db show <name> --url
#   turso db tokens create <name>
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Public site origin, used for metadata, sitemap and OG image URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
